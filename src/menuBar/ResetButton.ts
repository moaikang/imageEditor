import {Button} from "../component/Button";

export class ResetButton extends Button {
    constructor() {
        super({
            width: 70,
            height: 30,
            text: 'Reset'
        });
    }
}
